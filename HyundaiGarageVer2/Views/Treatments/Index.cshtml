@model IEnumerable<HyundaiGarageVer2.Models.Treatment>

@{
    ViewBag.Title = "Treatments";
}

<h2>Treatments</h2>

<p>
    @Html.ActionLink("Statistics", "TreatmentStatistics", "Treatments")
</p>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Car.CarID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Car.Model)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WorkHours)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TreatmentDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Car.CarID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Car.Model)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WorkHours)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TreatmentDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TreatmentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TreatmentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TreatmentID })
        </td>
    </tr>
}

</table>
